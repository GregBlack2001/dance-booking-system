<!DOCTYPE html>
<html>
<head>
  <title>Add New Course</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <h1 class="mb-4">{{#course}}Edit{{/course}}{{^course}}Add{{/course}} Course</h1>

<form method="POST" action="{{#course}}/courses/edit/{{course._id}}{{/course}}{{^course}}/courses/add{{/course}}">
  <div class="mb-3">
    <label class="form-label">Course Title</label>
    <input name="title" class="form-control" value="{{course.title}}" required>
  </div>

  <div class="mb-3">
    <label class="form-label">Instructor</label>
    <input name="instructor" class="form-control" value="{{course.instructor}}" required>
  </div>

  <div class="mb-3">
    <label class="form-label">Date</label>
    <input type="date" name="date" class="form-control" value="{{course.date}}" required>
  </div>

  <div class="mb-3">
    <label class="form-label">Description</label>
    <textarea name="description" class="form-control" required>{{course.description}}</textarea>
  </div>

  <button type="submit" class="btn btn-primary">Save</button>
  <a href="/courses" class="btn btn-secondary ms-2">Cancel</a>
</form>

</body>
</html>
