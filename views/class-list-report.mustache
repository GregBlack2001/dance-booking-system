<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{title}}</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    @media print {
      .no-print { display: none; }
      body { margin: 0; padding: 15px; }
      .container { width: 100%; max-width: 100%; }
    }
  </style>
</head>
<body>
  <div class="container py-4">
    <div class="no-print mb-3">
      <a href="/courses" class="btn btn-secondary">Back to Courses</a>
      <button onclick="window.print()" class="btn btn-primary ms-2">Print Report</button>
    </div>
    
    <div class="card">
      <div class="card-header text-center">
        <h3>Class Attendance List</h3>
      </div>
      <div class="card-body">
        <div class="row mb-4">
          <div class="col-md-6">
            <h5>Course Details</h5>
            <p><strong>Title:</strong> {{course.title}}<br>
              <strong>Date:</strong> {{course.date}}<br>
              <strong>Instructor:</strong> {{course.instructor}}<br>
              <strong>Location:</strong> {{course.location}}</p>
          </div>
          <div class="col-md-6 text-md-end">
            <h5>Report Details</h5>
            <p><strong>Generated on:</strong> {{generateDate}}<br>
              <strong>Total Participants:</strong> {{bookings.length}}</p>
          </div>
        </div>
        
        <h5>Participant List</h5>
        <table class="table table-bordered">
          <thead>
            <tr>
              <th>#</th>
              <th>Name</th>
              <th>Email</th>
              <th>Attendance</th>
            </tr>
          </thead>
          <tbody>
            {{#bookings}}
              <tr>
                <td>{{@index}}</td>
                <td>{{name}}</td>
                <td>{{email}}</td>
                <td style="width: 150px;"></td>
              </tr>
            {{/bookings}}
          </tbody>
        </table>
        
        <div class="mt-4">
          <h6>Notes:</h6>
          <p>_____________________________________________________________</p>
          <p>_____________________________________________________________</p>
          <p>_____________________________________________________________</p>
        </div>
      </div>
    </div>
  </div>
</body>
</html>