<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{title}}</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

  {{> navbar}}

  <div class="container py-5">
    <h1 class="mb-4 text-center">All Bookings</h1>

    {{#bookings.length}}
      <div class="table-responsive">
        <table class="table table-striped table-hover">
          <thead>
            <tr>
              <th>Course</th>
              <th>Date</th>
              <th>Name</th>
              <th>Email</th>
              <th>Booking Date</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            {{#bookings}}
              <tr>
                <td>{{course.title}}</td>
                <td>{{course.date}}</td>
                <td>{{name}}</td>
                <td>{{email}}</td>
                <td>{{bookingDate}}</td>
                <td>
                  <form method="POST" action="/booking/{{_id}}/cancel" class="d-inline">
                    <input type="hidden" name="_csrf" value="{{csrfToken}}">
                    <button type="submit" class="btn btn-sm btn-outline-danger">Cancel</button>
                  </form>
                </td>
              </tr>
            {{/bookings}}
          </tbody>
        </table>
      </div>
    {{/bookings.length}}

    {{^bookings.length}}
      <div class="alert alert-info text-center">No bookings found.</div>
    {{/bookings.length}}
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>