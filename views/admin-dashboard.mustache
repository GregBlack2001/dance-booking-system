<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{title}}</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body class="bg-light">
  {{> navbar}}

  <div class="container py-5">
    <h1 class="mb-4 text-center">Admin Dashboard</h1>

    <div class="row text-center mb-5">
      <div class="col-md-4">
        <div class="card shadow-sm">
          <div class="card-body">
            <h5 class="card-title">Total Courses</h5>
            <p class="display-6">{{courses.length}}</p>
          </div>
        </div>
      </div>
      <div class="col-md-4">
        <a href="/courses/add" class="text-decoration-none">
          <div class="card shadow-sm bg-success text-white">
            <div class="card-body">
              <h5 class="card-title">Add New Course</h5>
              <p class="lead">Create a new class or workshop</p>
            </div>
          </div>
        </a>
      </div>
      <div class="col-md-4">
        <a href="/classes" class="text-decoration-none">
          <div class="card shadow-sm bg-primary text-white">
            <div class="card-body">
              <h5 class="card-title">View All Classes</h5>
              <p class="lead">See your public listings</p>
            </div>
          </div>
        </a>
      </div>
    </div>

    <h3 class="mb-3">Manage Courses</h3>

    <div class="row">
      {{#courses}}
      <div class="col-md-6">
        <div class="card mb-4 shadow-sm">
          <div class="card-body">
            <h5 class="card-title">{{title}}</h5>
            <p><strong>Date:</strong> {{date}} <br>
               <strong>Location:</strong> {{location}}</p>
            <a href="/courses/edit/{{_id}}" class="btn btn-outline-secondary btn-sm">Edit</a>
            <form action="/courses/delete/{{_id}}" method="POST" class="d-inline">
              <button type="submit" class="btn btn-outline-danger btn-sm">Delete</button>
            </form>
            <a href="/participants/{{_id}}" class="btn btn-outline-info btn-sm">Participants</a>
          </div>
        </div>
      </div>
      {{/courses}}
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
