<!DOCTYPE html>
<html>
<head>
  <title>Courses</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  {{> navbar}}
  <h1 class="mb-4">Dance Courses</h1>

<ul class="list-group mb-3">
  {{#courses}}
    <li class="list-group-item">
      <h5 class="mb-1">{{title}}</h5>
      <small class="text-muted">Instructor: {{instructor}} on {{date}}</small>
      <p class="mb-1">{{description}}</p>

      {{#isAdmin}}
        <a href="/courses/edit/{{_id}}" class="btn btn-sm btn-outline-primary me-2">Edit</a>
        <form method="POST" action="/courses/delete/{{_id}}" style="display:inline;">
          <button class="btn btn-sm btn-outline-danger" type="submit">Delete</button>
        </form>
      {{/isAdmin}}
    </li>
  {{/courses}}
</ul>

{{#isAdmin}}
  <a href="/courses/add" class="btn btn-success">Add New Course</a>
  <a href="/participants/{{_id}}" class="btn btn-sm btn-outline-info ms-2">Participants</a>
{{/isAdmin}}
<a href="/logout" class="btn btn-secondary ms-3">Logout</a>

</body>
</html>

